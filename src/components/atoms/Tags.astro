---
import { getPermalink } from '~/utils/permalinks';

const { tags, class: className = 'text-xs' } = Astro.props;
---

{
	tags && Array.isArray(tags) && (
		<ul class={className}>
			{tags.map((tag) => (
				<li class="bg-zinc-100 dark:bg-zinc-700 inline-block mr-2 mb-2 py-0.5 px-2 rounded-md">
					<a href={getPermalink(tag, 'tag')}>{tag}</a>
				</li>
			))}
		</ul>
	)
}
